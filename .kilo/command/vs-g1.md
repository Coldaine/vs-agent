---
description: Run one reflex-only G1 episode
agent: code
---

From the repository root, run `.venv\Scripts\python.exe spine\run.py --reflex-only --disable-leader`.
Use no model credentials. Report the episode outcome JSON and never leave held movement keys after
the command exits.
